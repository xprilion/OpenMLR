import type { ExperimentRun } from './types';

export const INITIAL_MOCK_RUNS: ExperimentRun[] = [
  {
    id: 'run-gpt2-flashattn-01',
    name: 'nanoGPT-124M + FlashAttention-2',
    description: 'Ablation study replacing standard causal self-attention with FlashAttention v2 on OpenWebText.',
    status: 'running',
    started_at: '2026-08-20 01:15:00',
    duration_seconds: 2840,
    compute_target: 'Local H100 GPU (80GB)',
    tags: ['ablation', 'flashattention', 'gpt2', 'speedup'],
    hyperparameters: {
      model_architecture: 'GPT-2 (124M)',
      learning_rate: 0.0006,
      batch_size: 64,
      optimizer: 'AdamW',
      weight_decay: 0.1,
      warmup_steps: 200,
      max_epochs: 10,
      precision: 'bf16',
      gradient_accumulation_steps: 4,
      seed: 42,
    },
    current_step: 420,
    total_steps: 1000,
    current_epoch: 4,
    total_epochs: 10,
    best_val_loss: 2.842,
    best_val_accuracy: 0.468,
    metrics: {
      train_loss: Array.from({ length: 42 }, (_, i) => ({
        step: (i + 1) * 10,
        epoch: Math.floor(i / 10) + 1,
        timestamp: 1771463700000 + i * 60000,
        value: 4.8 / (1 + Math.log(i + 2) * 0.45) + (Math.sin(i) * 0.04),
      })),
      val_loss: Array.from({ length: 9 }, (_, i) => ({
        step: (i + 1) * 50,
        epoch: Math.floor((i * 50) / 100) + 1,
        timestamp: 1771463700000 + i * 300000,
        value: 4.6 / (1 + Math.log(i * 5 + 2) * 0.4) + 0.1,
      })),
      learning_rate: Array.from({ length: 42 }, (_, i) => ({
        step: (i + 1) * 10,
        epoch: Math.floor(i / 10) + 1,
        timestamp: 1771463700000 + i * 60000,
        value: i < 20 ? (0.0006 * (i + 1)) / 20 : 0.0006 * 0.5 * (1 + Math.cos((Math.PI * (i - 20)) / 22)),
      })),
      val_accuracy: Array.from({ length: 9 }, (_, i) => ({
        step: (i + 1) * 50,
        epoch: Math.floor((i * 50) / 100) + 1,
        timestamp: 1771463700000 + i * 300000,
        value: 0.18 + (i * 0.035),
      })),
    },
    hardware_history: Array.from({ length: 20 }, (_, i) => ({
      step: (i + 1) * 20,
      timestamp: 1771463700000 + i * 120000,
      gpu_util: 94 + (Math.sin(i) * 4),
      gpu_vram_mb: 28400 + (i * 120),
      gpu_temp: 64 + (i * 0.4),
      power_w: 320 + (Math.sin(i) * 15),
      cpu_util: 42 + (Math.cos(i) * 6),
    })),
    latest_hardware: {
      gpu_utilization_pct: 96,
      gpu_memory_used_mb: 30800,
      gpu_memory_total_mb: 81920,
      gpu_temperature_c: 67,
      power_draw_watts: 335,
      cpu_utilization_pct: 46,
      ram_used_gb: 34.2,
      ram_total_gb: 128.0,
    },
    checkpoints: [
      {
        id: 'ckpt-01',
        run_id: 'run-gpt2-flashattn-01',
        name: 'step_200.safetensors',
        step: 200,
        epoch: 2,
        val_loss: 3.114,
        accuracy: 0.385,
        file_size_bytes: 498200000,
        format: 'safetensors',
        sha256: 'a1b2c3d4e5f67890123456789abcdef0123456789abcdef0123456789abcdef0',
        created_at: '2026-08-20 01:25:00',
        is_best: false,
      },
      {
        id: 'ckpt-02',
        run_id: 'run-gpt2-flashattn-01',
        name: 'best_val_loss_step_400.safetensors',
        step: 400,
        epoch: 4,
        val_loss: 2.842,
        accuracy: 0.468,
        file_size_bytes: 498200000,
        format: 'safetensors',
        sha256: '9f8e7d6c5b4a3210fedcba9876543210fedcba9876543210fedcba9876543210',
        created_at: '2026-08-20 01:45:00',
        is_best: true,
      },
    ],
    logs: [
      '[01:15:00] [INFO] Initializing distributed training on 1x NVIDIA H100 80GB HBM3...',
      '[01:15:02] [INFO] Loading dataset openwebtext with token shard cache (12.4M tokens).',
      '[01:15:05] [INFO] Compiling model with torch.compile(backend="inductor", mode="max-autotune")...',
      '[01:15:20] [TRAIN] Step 10/1000 | Loss: 4.782 | LR: 3.00e-05 | 148.2 tokens/ms',
      '[01:18:40] [TRAIN] Step 100/1000 | Loss: 3.654 | LR: 3.00e-04 | 152.4 tokens/ms',
      '[01:25:10] [VAL] Step 200 | Val Loss: 3.114 | Val Acc: 38.5% | Checkpoint saved to step_200.safetensors',
      '[01:45:02] [VAL] Step 400 | Val Loss: 2.842 | Val Acc: 46.8% | New best val loss! Saved checkpoint.',
    ],
  },
  {
    id: 'run-gpt2-baseline-00',
    name: 'nanoGPT-124M Standard Baseline',
    description: 'Baseline run using standard PyTorch multi-head attention without kernel fusion.',
    status: 'completed',
    started_at: '2026-08-19 21:00:00',
    ended_at: '2026-08-19 22:45:00',
    duration_seconds: 6300,
    compute_target: 'Local H100 GPU (80GB)',
    tags: ['baseline', 'standard-mha', 'gpt2'],
    hyperparameters: {
      model_architecture: 'GPT-2 (124M)',
      learning_rate: 0.0006,
      batch_size: 64,
      optimizer: 'AdamW',
      weight_decay: 0.1,
      warmup_steps: 200,
      max_epochs: 10,
      precision: 'fp16',
      gradient_accumulation_steps: 4,
      seed: 42,
    },
    current_step: 1000,
    total_steps: 1000,
    current_epoch: 10,
    total_epochs: 10,
    best_val_loss: 2.915,
    best_val_accuracy: 0.452,
    metrics: {
      train_loss: Array.from({ length: 50 }, (_, i) => ({
        step: (i + 1) * 20,
        epoch: Math.floor(i / 5) + 1,
        timestamp: 1771448400000 + i * 120000,
        value: 4.9 / (1 + Math.log(i + 2) * 0.42) + (Math.cos(i) * 0.05),
      })),
      val_loss: Array.from({ length: 10 }, (_, i) => ({
        step: (i + 1) * 100,
        epoch: i + 1,
        timestamp: 1771448400000 + i * 600000,
        value: 4.8 / (1 + Math.log(i * 5 + 2) * 0.38) + 0.12,
      })),
      learning_rate: Array.from({ length: 50 }, (_, i) => ({
        step: (i + 1) * 20,
        epoch: Math.floor(i / 5) + 1,
        timestamp: 1771448400000 + i * 120000,
        value: i < 10 ? (0.0006 * (i + 1)) / 10 : 0.0006 * 0.5 * (1 + Math.cos((Math.PI * (i - 10)) / 40)),
      })),
    },
    hardware_history: [],
    latest_hardware: {
      gpu_utilization_pct: 0,
      gpu_memory_used_mb: 1200,
      gpu_memory_total_mb: 81920,
      gpu_temperature_c: 42,
      power_draw_watts: 65,
      cpu_utilization_pct: 8,
      ram_used_gb: 16.4,
      ram_total_gb: 128.0,
    },
    checkpoints: [
      {
        id: 'ckpt-base-01',
        run_id: 'run-gpt2-baseline-00',
        name: 'model_final.safetensors',
        step: 1000,
        epoch: 10,
        val_loss: 2.915,
        accuracy: 0.452,
        file_size_bytes: 498200000,
        format: 'safetensors',
        created_at: '2026-08-19 22:45:00',
        is_best: true,
      },
    ],
    logs: [
      '[21:00:00] [INFO] Baseline training started.',
      '[22:45:00] [INFO] Training completed in 105 mins. Final Val Loss: 2.915.',
    ],
  },
];
