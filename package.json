{
  "name": "openmlr-workspace",
  "private": true,
  "version": "0.4.0",
  "description": "OpenMLR — ML research intern (frontend workspace)",
  "scripts": {
    "dev:frontend": "pnpm --filter openmlr-frontend dev",
    "build:frontend": "pnpm --filter openmlr-frontend build"
  }
}
